services:
  - type: web
    name: forgegrid
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: FORGEGRID_WORK_SCALE
        value: "2"
